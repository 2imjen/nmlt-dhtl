// Handle files
#include <stdio.h>

int main() {

    // Write files
    /*FILE *fP = fopen("test.txt", "a");

    fprintf(fP, "\nYêu là cưới, trên dưới 2 nhà làm xui");

    fclose(fP);*/

    //check exist file
    //if file exists, func remove() will return 0
    /*if(remove("test.txt") == 0) {
        printf("Đã xóa file r thằng ngu Việt");
    } else {
        printf("Mày xóa file l nào thế");
    }*/

    //Reading files

    return 0;
}